export enum UserRole {
    Talent = 1,
    Recruiter = 2,
    Representative = 3
}

export enum JobType {
    FullTime = 1,
    PartTime = 2,
    Internship = 3,
    Contract = 4,
    Freelance = 5
}

export enum ToastType {
    Success = 'success',
    Error = 'error',
    Warning = 'warning',
    Info = 'info',
}

export enum ModalActionType {
    Create = 1,
    Update = 2
}